#include "FileManager.h"

namespace jer
{    
    /*const shared_ptr<FileLoadable> FileManager::access(const string &name, FACTORY_FUNC factory)
    {
        if(this->count(name))
            return this->at(name);
        else
        {
            FileLoadable *file = factory(name);
            if(file == NULL)
                return nullptr;
            return add(name, file);
        }
    }
    */
}